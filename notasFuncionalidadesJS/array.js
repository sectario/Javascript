var elementos = ["comida", "etc"];

console.log(elementos);

console.log(elementos.length);

var masElementos = elementos.push("carro");
//agrega elementos al final

var eliminarUltimo = elementos.pop("carro");

var agregarInicio = elementos.unshift("teclado");

var eliminarInicio = elementos.shift("teclado");